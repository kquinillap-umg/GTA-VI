<section
  class="jason relative z-10 lg:ps-40 2xl:ps-80 ps-10 py-40 mt-60 flex lg:flex-row flex-col justify-between gap-5 w-dvw overflow-x-hidden"
>
  <div class="max-w-lg">
    <h1 class="text-yellow font-long uppercase text-8xl mb-20 whitespace-nowrap">Jason Duval</h1>
    <h2 class="text-pink md:text-5xl text-3xl mb-7 md:pe-20 pe-10">
      Jason quiere una vida fácil, pero las cosas no hacen más que complicarse.
    </h2><p class="text-white md:text-2xl text-lg md:pe-28 pe-14">
      Jason se crio rodeado de estafadores y delincuentes. Tras un tiempo en el ejército intentando
      dejar atrás su adolescencia problemática, acabó en los cayos haciendo lo que mejor sabe:
      trabajar para narcotraficantes de la zona. Quizás haya llegado el momento de probar algo
      nuevo.
    </p>

    <div class="jason-2 bg-yellow h-[90vh] w-auto md:mt-36 mt-20 -translate-x-5">
      <img
        class="size-full object-cover [object-position:80%_center] hover:scale-x-[0.97] hover:scale-y-[0.98] transition duration-700 ease-in-out"
        src="/images/jason-2.webp"
      />
    </div>
  </div>

  <div class="space-y-5 mt-96 img-box">
    <div class="jason-1 bg-yellow lg:h-[80vh] w-auto -translate-x-5">
      <img
        class="size-full object-cover [object-position:5%_center] hover:scale-[0.98] transition duration-700 ease-in-out"
        src="/images/jason-1.webp"
      />
    </div>
    <div class="jason-3 bg-yellow h-[50vh] md:w-[60%] -translate-x-5">
      <img
        class="size-full object-cover [object-position:42%_center] hover:scale-[0.97] transition duration-700 ease-in-out"
        src="/images/jason-3.webp"
      />
    </div>
  </div>
</section>

<style scoped>
  .jason-content {
    background: radial-gradient(ellipse, #111117 20%, transparent 70%);
  }
</style>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  gsap.set('.jason', { marginTop: '-80vh' });

  gsap
    .timeline({
      scrollTrigger: {
        trigger: '.jason',
        start: 'top 90%',
        end: '10% center',
        scrub: 2,
      },
    })
    .to('.first-vd', { opacity: 0, duration: 1, ease: 'power1.inOut' });

  gsap.to('.jason .img-box', {
    scrollTrigger: {
      trigger: '.jason',
      start: 'top center',
      end: '80% center',
      scrub: 2,
    },
    y: -300,
    duration: 1,
    ease: 'power1.inOut',
  });
</script>
